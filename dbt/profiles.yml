invoice_pipeline:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: invoiceanalysispipeline  
      dataset: invoices              
      location: EU
      keyfile: /workspace/.gcp-service-account.json
      threads: 4
      timeout_seconds: 900