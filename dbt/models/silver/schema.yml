version: 2

models:
  - name: ds1_invoice_lines
    columns:
      - name: unique_invoice_line_no
        tests:
          - unique
          - not_null
      - name: unique_invoice_no
        tests:
          - not_null
  - name: ds2_invoice_lines
    columns: 
      #No point in adding tests for unique_invoice_line_no and unique_invoice_no as they are generated by dbt utils with surrogate key
      - name: order_id
        tests:
          - not_null